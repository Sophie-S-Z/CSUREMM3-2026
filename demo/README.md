# Demo

This folder is reserved for example/synthetic-data demonstrations — something that illustrates the *shape* of the modeling approach (e.g., on a small synthetic network) without using real study data, tuned parameters, or the full private pipeline.
